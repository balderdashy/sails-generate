/**
 * Route Mappings
 * (sails.config.routes)
 *
 * Your routes tell Sails what to do each time it receives a request.
 *
 * For more information on configuring custom routes, check out:
 * https://sailsjs.com/anatomy/config/routes-js
 */

module.exports.routes = {

<% if ((typeof frontend === 'undefined' || frontend !== false) && typeof (without == undefined || !_.contains(without, 'views'))) { %>
  //  ╦ ╦╔═╗╔╗ ╔═╗╔═╗╔═╗╔═╗╔═╗
  //  ║║║║╣ ╠╩╗╠═╝╠═╣║ ╦║╣ ╚═╗
  //  ╚╩╝╚═╝╚═╝╩  ╩ ╩╚═╝╚═╝╚═╝
<% if(!caviar) {%>
  /***************************************************************************
  *                                                                          *
  * Make the view located at `views/homepage.ejs` your home page.            *
  *                                                                          *
  * (Alternatively, remove this and add an `index.html` file in your         *
  * `assets` directory)                                                      *
  *                                                                          *
  ***************************************************************************/

  '/': {
    view: 'pages/homepage'
  },<%}
else { %>  'GET /':                   { action: 'misc/view-homepage' },// TODO

  'GET /faq':                { view:   'pages/faq' },
  'GET /legal/terms':        { action: 'legal/view-terms'},
  'GET /legal/privacy':      { view:   'pages/legal/privacy' },
  'GET /contact':            { view:   'pages/contact' },

  'GET /signup':             { action: 'entry/view-signup' },
  'GET /email/confirm':      { action: 'entry/confirm-email' },
  'GET /email/confirmed':    { view:   'pages/entry/confirmed-email' },

  'GET /login':              { action: 'entry/view-login' },
  'GET /password/forgot':    { action: 'entry/view-forgot-password' },
  'GET /password/new':       { action: 'entry/view-new-password' },

  'GET /account':            { action: 'account/view-my-account' },
  'GET /account/password':   { action: 'account/view-change-password' },
  'GET /account/profile':    { action: 'account/view-profile' },<% } %>
<% } %>
<% if(!caviar) { %>  /***************************************************************************
  *                                                                          *
  * More custom routes here...                                               *
  * (See https://sailsjs.com/config/routes for examples.)                    *
  *                                                                          *
  * If a request to a URL doesn't match any of the routes in this file, it   *
  * is matched against "shadow routes" (e.g. blueprint routes).  If it does  *
  * not match any of those, it is matched against static assets.             *
  *                                                                          *
  ***************************************************************************/

<% } %>
  //  ╔═╗╔═╗╦  ╔═╗╔╗╔╔╦╗╔═╗╔═╗╦╔╗╔╔╦╗╔═╗
  //  ╠═╣╠═╝║  ║╣ ║║║ ║║╠═╝║ ║║║║║ ║ ╚═╗
  //  ╩ ╩╩  ╩  ╚═╝╝╚╝═╩╝╩  ╚═╝╩╝╚╝ ╩ ╚═╝
<% if(caviar) {%>  // Note that, in this app, these API endpoints may be accessed using the `Cloud.*()` methods
  // from the CloudSDK library.
  '/api/v0.0/account/logout':                           { action: 'account/logout' },
  'PUT   /api/v0.0/account/update-password':            { action: 'account/update-password' },
  'PUT   /api/v0.0/account/update-profile':             { action: 'account/update-profile' },
  'PUT   /api/v0.0/account/update-billing-card':        { action: 'account/update-billing-card' },
  'PUT   /api/v0.0/entry/login':                        { action: 'entry/login' },
  'POST  /api/v0.0/entry/signup':                       { action: 'entry/signup' },
  'POST  /api/v0.0/entry/send-password-recovery-email': { action: 'entry/send-password-recovery-email' },
  'POST  /api/v0.0/entry/update-password-and-login':    { action: 'entry/update-password-and-login' },
  'POST  /api/v0.0/misc/contact':                       { action: 'misc/contact' },<%}%>


  //  ╦ ╦╔═╗╔╗ ╦ ╦╔═╗╔═╗╦╔═╔═╗
  //  ║║║║╣ ╠╩╗╠═╣║ ║║ ║╠╩╗╚═╗
  //  ╚╩╝╚═╝╚═╝╩ ╩╚═╝╚═╝╩ ╩╚═╝


<% if(!caviar) { %>  //  ╔╦╗╦╔═╗╔═╗
  //  ║║║║╚═╗║
  //  ╩ ╩╩╚═╝╚═╝
<% }
else { %>  //  ╔╦╗╦╔═╗╔═╗  ╦═╗╔═╗╔╦╗╦╦═╗╔═╗╔═╗╔╦╗╔═╗
  //  ║║║║╚═╗║    ╠╦╝║╣  ║║║╠╦╝║╣ ║   ║ ╚═╗
  //  ╩ ╩╩╚═╝╚═╝  ╩╚═╚═╝═╩╝╩╩╚═╚═╝╚═╝ ╩ ╚═╝
  '/terms':                   '/legal/terms',
  '/logout':                  '/api/v0.0/account/logout',<% } %>

};
